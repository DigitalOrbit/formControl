import { StreetValidatorDirective } from './street-validator.directive';

describe('StreetValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StreetValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
